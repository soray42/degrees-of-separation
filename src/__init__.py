"""Degrees of Separation — source package.

Submodules:
    crosswalks   CIP-centred crosswalks (the join key across all data sources).
    ar_pipeline  ORCID+OpenAlex -> directed hiring network -> SpringRank (Step 5).
"""
