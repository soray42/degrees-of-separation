# Degrees of Separation — pinned Python dependencies (reproduction)
# Tested on Python 3.12.3. Install into a clean virtual environment:
#   python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# Exact pins are the tested configuration. SpringRank is the canonical
# De Bacco-Larremore-Moore package (https://github.com/LarremoreLab/SpringRank);
# a self-contained NumPy/SciPy implementation also lives in src/ar_pipeline/springrank.py.

# core scientific stack
numpy==2.4.6
pandas==3.0.3
scipy==1.18.0
statsmodels==0.14.6
patsy==1.0.2
networkx==3.6.1
scikit-learn==1.9.0      # 1-D GMM + ARI/NMI clustering metrics (scripts/47)

# IO / data access
requests==2.34.2
pyarrow==24.0.0
openpyxl==3.1.5          # College Scorecard / NCSES .xlsx tables
xlrd==2.0.2              # legacy .xls (older NCSES releases)
python-dateutil==2.9.0.post0

# plotting
matplotlib==3.11.0

# academic-reputation ranks
SpringRank==0.0.10
